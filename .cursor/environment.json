{
  "name": "DevFlow AI Advertising Studio",
  "install": "cp -n .env.example .env || true; npm install && npx prisma db push && npm run db:seed",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Next.js dev server (App Router) on http://localhost:3000"
    }
  ],
  "ports": [
    { "name": "web", "port": 3000 }
  ]
}
