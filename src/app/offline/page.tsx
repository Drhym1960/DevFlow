export default function OfflinePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-lg flex-col justify-center px-6">
      <p className="text-xs uppercase tracking-[0.28em] text-gold-400">Offline</p>
      <h1 className="mt-3 font-display text-4xl">The studio is cached, the render desk is not.</h1>
      <p className="mt-4 text-mist-300">Reconnect to write scripts and finish films.</p>
    </main>
  );
}
